<VirtualHost *:80>
    ServerName decocake.usercodex.com

    DocumentRoot /home/usercodex/decocake_front
    <Directory /home/usercodex/decocake_front>
        Options -Indexes +FollowSymLinks
        AllowOverride None
        Require all granted
        FallbackResource /index.html
    </Directory>

    ProxyPreserveHost On
    ProxyPass /api/ http://127.0.0.1:8004/api/
    ProxyPassReverse /api/ http://127.0.0.1:8004/api/

    ProxyPass /media/ !
    Alias /media/ /home/usercodex/decocake_src/Backend/media/
    <Directory /home/usercodex/decocake_src/Backend/media>
        Options -Indexes
        Require all granted
    </Directory>

    Alias /static/ /home/usercodex/decocake_src/Backend/staticfiles/
    <Directory /home/usercodex/decocake_src/Backend/staticfiles>
        Require all granted
    </Directory>

    ErrorLog ${APACHE_LOG_DIR}/decocake_error.log
    CustomLog ${APACHE_LOG_DIR}/decocake_access.log combined
</VirtualHost>
